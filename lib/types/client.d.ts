/**
 * dsh-memory Web client bundle (`@dsh-external/dsh-memory/client`).
 *
 * A `window.__ModuleLoader__.load` bundle registering the "Memory" card on the
 * `settings.plugin.item` slot. Loaded by the DSH client module system via the
 * package's `dsh.client` declaration.
 */
export declare const inject: ['slots']
export declare function apply(ctx: unknown): void
